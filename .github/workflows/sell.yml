name: 10:01 Sell
on:
  schedule:
    - cron: '1 2 * * 1-5'    # 周一至周五 02:01 UTC = 10:01 CST
  workflow_dispatch:

jobs:
  sell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Settle and sell
        run: python3 scripts/render_cron_call.py sell
